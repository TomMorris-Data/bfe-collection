name = "bfe-health-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[triggers]
# Monday 8am UTC — weekly dispatch
crons = ["0 8 * * MON"]

[vars]
APP_BASE_URL = "https://bfe-health-frontend.pages.dev"
ENVIRONMENT = "production"
SUPABASE_URL = "https://qmrbuqajdbxubfddifre.supabase.co"

# Secrets — set in Cloudflare dashboard → Workers → bfe-health-api → Settings → Variables & Secrets
# SUPABASE_SERVICE_ROLE_KEY
# RESEND_API_KEY
# ANTHROPIC_API_KEY
